apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myapp-data
  namespace: scc-lab
spec:
  accessModes:
    - ReadWriteOnce          # One pod can read/write at a time
  resources:
    requests:
      storage: 1Gi           # Request 1 GB